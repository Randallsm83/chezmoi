{{- /*
  1Password SSH agent configuration.
  Managed by chezmoi — edit:
    .chezmoitemplates/1password-agent.toml   (this file, content)
    .chezmoidata.yaml -> ssh.onepassword_vaults  (vault list)

  Targets:
    Windows: %LOCALAPPDATA%\1Password\config\ssh\agent.toml
    macOS:   ~/.config/1Password/ssh/agent.toml
    Linux:   ~/.config/1Password/ssh/agent.toml

  Docs: https://developer.1password.com/docs/ssh/agent/config
*/ -}}
# Managed by chezmoi — do NOT edit by hand.
# Source: .chezmoitemplates/1password-agent.toml (vault list in .chezmoidata.yaml)
#
# Order of [[ssh-keys]] blocks below = order keys are offered to SSH servers.
# Verify with: ssh-add -l
{{ range .ssh.onepassword_vaults }}
[[ssh-keys]]
vault = "{{ . }}"
{{ end -}}
