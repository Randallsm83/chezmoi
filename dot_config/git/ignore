#  ██████╗ ██╗████████╗
# ██╔════╝ ██║╚══██╔══╝
# ██║  ███╗██║   ██║
# ██║   ██║██║   ██║
# ╚██████╔╝██║   ██║
#  ╚═════╝ ╚═╝   ╚═╝
# Distributed version control system.
#

# Global gitignore
# Managed by chezmoi

# === OS-specific ===

# macOS
.DS_Store
.AppleDouble
.LSOverride
._*

# Windows
Thumbs.db
Thumbs.db:encryptable
ehthumbs.db
ehthumbs_vista.db
Desktop.ini
$RECYCLE.BIN/

# Linux
*~
.directory
.Trash-*

# === Editors & IDEs ===

# VSCode
.vscode/
*.code-workspace
# VSCode "Local History" extension cache
.history/

# JetBrains IDEs
.idea/
*.iml
*.iws
*.ipr

# Zed
.zed/

# Helix
.helix/

# Vim
[._]*.s[a-v][a-z]
[._]*.sw[a-p]
[._]s[a-rt-v][a-z]
[._]ss[a-gi-z]
[._]sw[a-p]
Session.vim
Sessionx.vim
.netrwhist
# `tags` removed from GLOBAL: too broad (any file named `tags` anywhere is hidden).
# ctags users should add `tags` to their per-repo .gitignore instead.
[._]*.un~

# Emacs
*~
\#*\#
/.emacs.desktop
/.emacs.desktop.lock
*.elc
auto-save-list
tramp
.\#*

# Sublime Text
*.tmlanguage.cache
*.tmPreferences.cache
*.stTheme.cache
*.sublime-workspace
*.sublime-project

# === Build & Dependencies ===

# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
.pnp.*
.npm
.yarn/cache
.yarn/unplugged
.yarn/build-state.yml
.yarn/install-state.gz
# Bundler / framework / dev-server caches
.next/
.nuxt/
.svelte-kit/
.astro/
.turbo/
.vite/
.parcel-cache/
.cache/
dist-ssr/
.docusaurus/
# Deploy preview state
.vercel/
.netlify/
# Test/coverage
.nyc_output/
coverage/
*.lcov
lcov.info

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
# `build/`, `dist/`, `var/` removed from GLOBAL: words too generic and conflict
# with non-Python uses (JS/CMake/Bazel `build/`, web bundler `dist/`, Unix `var/`).
# Python projects: add to per-repo .gitignore.
develop-eggs/
downloads/
eggs/
.eggs/
/lib/
/lib64/
parts/
sdist/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST
*.manifest
*.spec
.pytest_cache/
.coverage
.coverage.*
.tox/
.nox/
htmlcov/
.hypothesis/
*.mo
*.pot
venv/
ENV/
env/
.venv
# Modern Python type/lint caches
.ruff_cache/
.mypy_cache/
.pyre/
.pytype/
.pyright/
.dmypy.json
dmypy.json
# uv / pip-tools / poetry runtime state
.uv/
pip-wheel-metadata/

# Ruby
*.gem
*.rbc
# `/.config` removed from GLOBAL: anchored to repo root but dotfiles repos often
# have a literal `.config` at root (XDG conventions). Ruby projects rarely need
# this; if they do, per-repo .gitignore.
/coverage/
/InstalledFiles
/pkg/
/spec/reports/
/spec/examples.txt
/test/tmp/
/test/version_tmp/
/tmp/
.bundle/
vendor/bundle
Gemfile.lock

# Go
*.exe
*.exe~
*.dll
*.so
*.dylib
*.test
*.out
go.work

# Rust
target/
**/*.rs.bk
*.pdb
# NOTE: Cargo.lock is intentionally listed here even though the convention
# is to commit it for binaries (bins) and ignore it for libraries (libs).
# Most one-off scratch repos under ~/projects are bin-like; the few lib
# repos override this with a `!Cargo.lock` in their own .gitignore.
Cargo.lock

# JVM (Java / Kotlin / Scala)
# No JVM work on this machine (as of 2026-05-17). Section kept for portability
# of this dotfiles repo, but `out/` and `bin/` are removed from GLOBAL because
# they are too generic and hide non-JVM content (e.g. chezmoi-managed `dot_local/bin/`).
# Restore them in per-Java-repo .gitignore if needed.
*.class
*.war
*.ear
.gradle/
.mvn/
hs_err_pid*.log
replay_pid*.log
# Scala
.bloop/
.metals/
.bsp/

# PHP
vendor/
composer.phar
.phpunit.result.cache

# Lua
*.luac
luac.out

# === Misc ===

# Logs
*.log
logs/
crash.log
crash.*.log

# Environment files
.env
.env.local
.env.*.local
# direnv per-directory shell loader cache
.direnv/
.envrc.local

# mise local-only overrides (the tracked file is mise.toml / .mise.toml)
mise.local.toml
.mise.local.toml
.tool-versions.local

# Temporary files
*.tmp
*.temp
*.swp
*.swo
*~

# Backup files
*.bak
*.backup
*.old

# Archives
*.zip
*.tar
*.tar.gz
*.rar
*.7z

# Certificates & Keys (NEVER commit these!)
*.pem
*.key
*.cert
*.crt
*.p12
*.pfx

# === Infra-as-code local state ===

# Terraform / OpenTofu
.terraform/
.terraform.tfstate.lock.info
*.tfstate
*.tfstate.*
# NOTE: .terraform.lock.hcl is intentionally NOT ignored - commit it.
.terragrunt-cache/

# Pulumi
.pulumi/

# Serverless Framework
.serverless/

# === Agent / tooling runtime state ===
.sisyphus/
# AI coding assistants (drop session/cache/history files in repos they touch)
.aider*
.cursor/
.continue/
.opencode/
.specstory/
# repomix CLI output dumps
repomix-output.*

# === macOS finder cruft we missed up top ===
Icon\r
