{{- /*
GitLab personal access token for ~/.config/gitlab/token.
Resolved via 1Password CLI at apply time. Requires an active op session.
Item: `Gitlab PAT ***REMOVED***` (vault: Personal, field: credential).

`private_` prefix → 0600 on Unix; no-op on Windows.
*/ -}}
{{ includeTemplate "op-read-safe" (dict "ref" "op://Personal/Gitlab PAT ***REMOVED***/credential" "os" .chezmoi.os) -}}
